import { fireEvent, screen, render } from '../utils/test-utils';
import App from '../App.js';

describe("Performance Page Integration Tests", () => {
    test("Entering a valid stock updates the state and renders a plot", () => {
        
    });

    test("Entering an invalid stock clears out state, empties Performance Page, and sets a message (Invalid Stock)", () => {

    });
})